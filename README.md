# Hermes — حافظه و لاگ پروژه‌های خودکارسازی ARIAMIR

این ریپو نقش «حافظه‌ی بلندمدت» پروژه‌های خودکارسازی آریامیر (AR_BRANDING) رو داره.
هر کاری که یه ایجنت (Claude) روی زیرساخت انجام می‌ده، اینجا به‌صورت خلاصه و ساختاریافته ثبت می‌شه
تا در جلسات بعدی، بدون از دست دادن context، ادامه داده بشه.

⚠️ **این ریپو Public است.** به همین دلیل هیچ کلید/توکن/رمز واقعی اینجا ذخیره نمی‌شه.
همه‌ی کلیدها (Telegram Bot Token, Gemini API Key) داخل جدول `bot_settings` روی
پروژه‌ی Supabase نگه‌داری می‌شن (نه اینجا).

## زیرساخت فعلی

**Supabase Project:** `kcmcovspbznredsiyqva` (region: ap-southeast-1)

### کانال‌های تلگرام تحت مدیریت
| کانال | Chat ID | نقش |
|---|---|---|
| `@Ariamir_academy` | `-1002637869503` | کانال اصلی برندینگ/آموزش |
| `@ARIAMIRZONE` | `-1003848584897` | کانال محتوای متنوع (خبر/دانستنی/انگیزشی/تاریخ/تبلیغ/آهنگ/نظرسنجی) |

### ربات تلگرام
`AR_AGENTBOT` — ادمین هر دو کانال بالا + چت‌بات شخصی (پاسخ‌دهی در پی‌وی با Gemini)

بقیه‌ی جزئیات در `MEMORY_LOG.md` و `ARCHITECTURE.md`.
