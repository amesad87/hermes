# لاگ تاریخچه‌ی کار (Session Log)

## ۲۰۲۶-۰۸-۳۰

### ۱. شروع: ایده‌ی اولیه‌ی ربات یوتیوب (منسوخ شد)
درخواست اولیه: ربات تلگرامی که هر روز ساعت ۱۰ ویدیوهای جدید AI/آموزشی یوتیوب رو به پی‌وی بفرسته.
- تلاش برای گرفتن YouTube Data API Key ناموفق بود (Google Cloud Console از ایران در دسترس نبود).
- به‌جاش روش scraping مستقیم HTML یوتیوب (بدون کلید) کشف و پیاده‌سازی شد — همچنان به‌عنوان زیرساخت مشترک استفاده می‌شه.
- **این ایده کنار گذاشته شد** و پروژه به سمت کانال ادمین رفت (بخش بعد).

### ۲. کانال ۱: Ariamir_academy
- ربات `AR_AGENTBOT` که از قبل ادمین کانال بود شناسایی و تأیید شد.
- سبک محتوایی کانال (از پست‌های واقعی قبلی) استخراج شد: ایموجی + تیتر + توضیح + ۳ بولت 🔹 + هشتگ‌ها.
- پایپ‌لاین خبر ساخته شد: RSS (TechCrunch/SEJ/Marketing Dive) → Gemini بازنویسی به سبک کانال → پست با عکس واقعی مقاله.
- پایپ‌لاین انگیزشی ساخته شد: پست سنگین فلسفی فارسی/انگلیسی رندوم، با عکس بازیافتی.
- کرون‌جاب‌ها فعال شدن.

### ۳. کانال ۲: ARIAMIRZONE (کانال جدید)
- کاربر کانال جدید ساخت و ربات رو ادمین کرد.
- نیازمندی‌ها: نظرسنجی، دانستنی، خبر، تبلیغ (کم)، آهنگ (لینک مخفی)، احساسات، انگیزشی، تاریخ ایران، افتخارات ایرانی‌های بزرگ.
- Dispatcher وزن‌دار ساخته شد (۹ نوع محتوا، تبلیغ کم‌وزن‌ترین ~۱ بار در روز).
- برای آهنگ: چون امکان ارسال فایل کپی‌رایتی نبود، از جستجوی مستقیم یوتیوب (بدون کلید) برای پیدا کردن لینک واقعی استفاده شد + Telegram HTML parse_mode برای مخفی‌کردن لینک.
- باگ رفع‌شده: تابع `yt_search_videos` نیاز به url-encode کوئری فارسی داشت (با `extensions.urlencode`).

### ۴. چت‌بات شخصی
- درخواست: امکان چت مستقیم با AI از طریق همون ربات.
- چون دیپلوی Edge Function (که برای Webhook لازمه) مدام با خطای «No approval received» مواجه می‌شد، به‌جاش poll-based (هر ۱ دقیقه چک `getUpdates`) پیاده‌سازی شد.
- حافظه‌ی مکالمه: آخرین ۱۰ پیام هر chat_id در جدول `chat_history`.

### ۵. رفع باگ‌ها و سخت‌گیری امنیتی
- باگ: یه migration قبلی «success» نشون داده بود ولی واقعاً apply نشده بود (ستون `image_url` گم شده بود) — پیدا و رفع شد.
- باگ: تداخل نام متغیر PL/pgSQL با نام ستون (`image_url`) در `run_motivational_post` — رفع شد با تغییر نام متغیر.
- هشدار امنیتی: RLS روی همه‌ی جدول‌های public غیرفعال بود (کلیدها بی‌محافظ). به تأیید کاربر فعال شد.

### ۶. این ریپو (Hermes)
- کاربر یه ریپوی گیت‌هاب (public, خالی) به اسم `hermes` معرفی کرد تا به‌عنوان حافظه‌ی بلندمدت پروژه استفاده بشه.
- ساختار اولیه (README + ARCHITECTURE + این لاگ) ساخته و commit شد.
- **قرارداد برای جلسات بعدی:** هر تغییر مهم زیرساختی، اینجا به همین فرمت (تاریخ + شرح خلاصه) اضافه بشه.

---
*این فایل توسط Claude (دستیار AI آریامیر) در طول این جلسه‌ی کاری تولید و به‌روزرسانی می‌شه.*
